import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drivinginstructor.app',
  appName: 'Driving Instructor Assistant',
  webDir: 'www'
};

export default config;
